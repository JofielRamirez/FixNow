package com.example.fixnow.ui.theme

import androidx.compose.ui.graphics.Color

// Naranja primario — botones, FABs, íconos activos, gradientes
val OrangePrimary    = Color(0xFFFFB300)

// Naranja claro — gradientes, fondos de header, acentos suaves
val OrangeLight      = Color(0xFFFFD54F)

// Naranja oscuro — botón presionado / variante de estado activo
val OrangeDark       = Color(0xFFCC8E00)

// Amarillo header — para el gradiente del header de inicio
val YellowHeader     = Color(0xFFFBC02D)
val YellowHeaderEnd  = Color(0xFFFFF176)

// ══════════════════════════════════════════
//  FONDOS Y SUPERFICIES
// ══════════════════════════════════════════
val BackgroundWhite  = Color(0xFFFAFAFA)
val BackgroundCard   = Color(0xFFFFFFFF)
val BackgroundLight  = Color(0xFFFDFDFD)

// ══════════════════════════════════════════
//  TEXTO
// ══════════════════════════════════════════
val TextPrimary      = Color(0xFF333333)
val TextSecondary    = Color(0xFF757575)
val TextOnOrange     = Color(0xFFFFFFFF)
val TextGray         = Color(0xFF9E9E9E)

// ══════════════════════════════════════════
//  ESTADOS
// ══════════════════════════════════════════
val ColorSuccess     = Color(0xFF4CAF50)   // reseñas positivas, confirmaciones
val ColorError       = Color(0xFFE53935)   // mensajes de error
val ColorNavUnselected = Color(0xFF9E9E9E) // íconos inactivos del BottomNav

// Acento secundario
val CyanAccent       = Color(0xFF00BCD4)

// Variante terciaria
val OrangeSecondary  = Color(0xFFFF8F00)

val BackgroundDark   = Color(0xFF121212)

// Superficies
val SurfaceLight     = Color(0xFFFFFFFF)
val SurfaceDark      = Color(0xFF1E1E1E)

// Texto
val TextBlack        = Color(0xFF212121)
val TextWhite        = Color(0xFFF5F5F5)

// Error
val ErrorRed         = Color(0xFFCF6679)
